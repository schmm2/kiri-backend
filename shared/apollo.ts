/*
import {
    ApolloClient,
    InMemoryCache
} from '@apollo/client';

var path = "http://localhost:7077/api/graphql";

const apolloClient = new ApolloClient({
    uri: path,
    cache: new InMemoryCache(),
});

export default apolloClient;*/